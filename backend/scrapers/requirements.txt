# Core HTTP & parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
urllib3>=2.0.0

# Anti-bot / anti-scraper bypass (primary: curl_cffi for TLS fingerprint impersonation)
curl_cffi>=0.6.0          # Mimics Chrome/Firefox TLS fingerprint — best general bypass
cloudscraper>=1.2.71      # Cloudflare JS-challenge solver (fallback)

# Optional: browser automation for heavy JS pages (install separately if needed)
# playwright>=1.44.0      # Full browser via Playwright (pip install playwright && playwright install chromium)
# DrissionPage>=4.0.0     # Chromium-based, simpler API than Playwright
